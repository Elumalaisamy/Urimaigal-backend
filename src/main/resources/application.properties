# ==========================================
# Urimaigal Backend Configuration
# ==========================================

server.port=8080
spring.application.name=Evaluation

# ==========================================
# MySQL Configuration
# ==========================================
spring.datasource.url=jdbc:mysql://192.168.16.191:5873/user_1206?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=rw_onprem_user_1206
spring.datasource.password=E3LU_MALA_I9SM_Y2CV_B5NT
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Connection Pool (HikariCP)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# Initialize schema on startup
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql
spring.sql.init.data-locations=classpath:data.sql
spring.sql.init.continue-on-error=true

# ==========================================
# Apache Artemis JMS Configuration
# ==========================================
spring.artemis.broker-url=tcp://localhost:61616
spring.artemis.user=Elumalai
spring.artemis.password=elu@2005
spring.artemis.pool.enabled=false

# JMS listener concurrency
spring.jms.is-pub-sub-domain=false
spring.jms.template.default-destination=my-queue
spring.jms.listener.auto-startup=true
spring.jms.listener.concurrency=1
spring.jms.listener.max-concurrency=5

# Queue names
app.jms.booking-queue=booking.consultation.queue
app.jms.notification-queue=notification.queue
app.jms.chat-queue=chat.message.queue

# ==========================================
# Elasticsearch Configuration
# ==========================================
spring.elasticsearch.uris=https://localhost:9200
spring.elasticsearch.username=elastic
spring.elasticsearch.password=elumalai
elasticsearch.host=localhost
elasticsearch.port=9200
elasticsearch.scheme=https
elasticsearch.username=elastic
elasticsearch.password=elumalai
elasticsearch.index.lawyers=lawyers
elasticsearch.index.consultations=consultations

# ==========================================
# JWT Security Configuration
# ==========================================
app.jwt.secret=urimaigal-super-secret-jwt-key-2024-legal-platform-secure-key
app.jwt.expiration-ms=86400000

# ==========================================
# CORS Configuration
# ==========================================
app.cors.allowed-origins=http://localhost:5173,http://localhost:3000,https://urimaigal-front-end.vercel.app,https://urimaigal-final-frontend-h8lt.vercel.app

# ==========================================
# Logging
# ==========================================
logging.level.com.urimaigal=DEBUG
logging.level.org.springframework.jdbc=DEBUG
logging.level.org.springframework.jms=DEBUG
